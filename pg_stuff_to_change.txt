Things to make compatible in queries:
ON DUPLICATE
REGEXP
COMPRESS() and DECOMPRESS()
Delete table1, table2 from 

Possible things to change:
$row = $db->queryOneRow("SELECT DATE_FORMAT(max(postdate), '%d/%m/%Y') AS postdate FROM releases");
$airdate = ($airdate > -1) ? sprintf(" AND releases.tvairdate >= DATE_SUB(CURDATE(), INTERVAL %d DAY) ", $airdate) : '';

Save tvrage images to disk. Or find a way to store them in pgsql.

Patch to remove releaseID from predb.
Patch to remove relstatus from releases.

Update tmux optimize.
