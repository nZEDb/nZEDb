Things to make compatible in queries:
ON DUPLICATE binaries.php line 628

Coalesce on the min / max etc files/size (stage 3 and 4.5) is wrong, it should be greatest/max.

/*
Releases.php line 255-280ish
and
users.php 650-670 ish

DATE_SUB

$airdate = ($airdate > -1) ? sprintf(" AND releases.tvairdate >= DATE_SUB(CURDATE(), INTERVAL %d DAY) ", $airdate) : '';
*/


/*
tmux monitor.php line 78
*/

/*
mysql : GROUP_CONCAT(column SEPARATOR ',')
pgsql : STRING_AGG(column, ',')
*/
